---
const { href, classes } = Astro.props;
---

<li
  class=`md:pb-3 font-noto-sans font-medium active:text-bumble hover:text-bumble transition-colors duration-300 text-h4 md:text-lead ${classes}`
>
  {
    href ? (
      <a href={href}>
        <slot />
      </a>
    ) : (
      <slot />
    )
  }
</li>
